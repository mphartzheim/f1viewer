[changelog]
header = "# Changelog"
body = """
{% for commit in commits %}
- {{ commit.message | trim }} ([{{ commit.hash | truncate(length=7, end="") }}])
{% endfor %}
"""
include_unreleased = true
trim = true

[git]
conventional_commits = false
split_commits = false
tag_pattern = "v[0-9]*"
